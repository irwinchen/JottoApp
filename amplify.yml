version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g expo-cli
        - yarn install
    build:
      commands:
        - expo export:web
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
